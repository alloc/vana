import { AnyArray } from '../shared';
/**
 * Emit a change for each index in the given range.
 */
export declare function commitIndices(base: AnyArray, copy: AnyArray, index: number, count: number): void;
